# AI模型选择功能测试说明

## 功能概述
在脚本生成页面新增了AI模型选择开关，用户可以选择使用O1或R1模型生成脚本，并在生成的脚本上显示模型信息。

## 功能特性

### 🎯 核心功能
- **简单开关设计**：使用小巧的开关按钮选择O1/R1模型
- **默认O1模型**：系统默认选择O1模型
- **模型标识显示**：每个生成的脚本都会显示使用的模型
- **智能模型分配**：不同任务类型自动分配不同模型

### 🎨 界面设计
- **简洁的开关设计**：小巧的滑动开关，O1/R1标签
- **直观的切换**：开关位置直观显示当前选择的模型
- **颜色区分**：O1模型使用蓝色标识，R1模型使用紫色标识

## 测试步骤

### 1. 启动项目
```bash
cd /Users/lucien.xu/Desktop/代码汇总/videotool
python -m http.server 8000
```

### 2. 访问页面
打开浏览器访问：http://localhost:8000

### 3. 测试模型选择功能
1. 点击左侧导航的"AI视频创作"
2. 进入脚本生成页面（步骤2）
3. 在左侧配置区域找到"AI模型"选择区域

### 4. 验证功能

#### 测试用例1：模型选择开关
- **O1模型选择**：
  - 开关在左侧位置（默认）
  - 预期结果：O1标签高亮，R1标签变灰
  - 开关背景为灰色

- **R1模型选择**：
  - 点击开关切换到右侧
  - 预期结果：R1标签高亮，O1标签变灰
  - 开关背景为蓝色

#### 测试用例2：脚本生成模型标识
- **生成新脚本**：
  - 选择O1模型，点击"立即生成"
  - 预期结果：生成的脚本标题显示"脚本X O1"标识
  - 标识颜色：蓝色背景

- **切换模型生成**：
  - 选择R1模型，点击"立即生成"
  - 预期结果：新生成的脚本显示"脚本X R1"标识
  - 标识颜色：紫色背景

#### 测试用例3：任务跳转模型标识
- **跳转任务**：
  - 点击任务按钮，选择任意任务
  - 预期结果：任务相关脚本显示对应的模型标识
  - 不同任务类型显示不同模型：
    - 儿童纯棉百搭小清新碎花上衣 → O1
    - 夏季T恤视频制作 → R1
    - 时尚连衣裙视频制作 → O1
    - 连衣裙推广视频 → O1

### 5. 功能验证点
- ✅ 模型选择开关正常工作
- ✅ 默认选择O1模型
- ✅ 模型切换实时生效
- ✅ 生成的脚本显示模型标识
- ✅ 模型标识颜色正确区分
- ✅ 任务跳转时模型标识正确显示
- ✅ 界面美观，交互流畅

## 技术实现

### 新增HTML结构
```html
<!-- 模型选择 -->
<div class="config-item">
    <label>AI模型</label>
    <div class="model-toggle">
        <span class="toggle-label">O1</span>
        <label class="toggle-switch">
            <input type="checkbox" id="modelToggle" onchange="toggleModel()">
            <span class="toggle-slider"></span>
        </label>
        <span class="toggle-label">R1</span>
    </div>
</div>
```

### 核心JavaScript函数
- `initializeAIModelSelection()`: 初始化模型选择功能
- `toggleModel()`: 切换模型函数
- `getModelForTask(task)`: 根据任务类型确定模型
- 脚本生成时自动添加模型标识

### 样式特性
- 简洁的滑动开关设计
- 平滑的切换动画效果
- 模型标识颜色区分
- 响应式设计

## 模型分配规则

### 默认模型
- 系统默认选择：O1
- 用户可手动切换：O1 ↔ R1

### 任务类型模型分配
- **儿童纯棉百搭小清新碎花上衣** → O1（复杂场景）
- **夏季T恤视频制作** → R1（简单场景）
- **时尚连衣裙视频制作** → O1（复杂场景）
- **连衣裙推广视频** → O1（复杂场景）

## 用户体验
- 直观的模型选择界面
- 清晰的模型功能描述
- 实时的模型标识显示
- 智能的模型分配逻辑
